

本项目是一个将**经典聪明钱理论 (SMC)** 与 **AI 多智能体决策 (AI Regime Classifier)** 深度融合的量化交易系统。它拥有极强的工程落地性与严谨的风险控制体系。

---

# AI-SMC 交易系统深度技术分析报告

## 一、 项目定位与业务流程 (Workflow)

[AI-SMC](file:///e:/bossquant/MQ5/AI-SMC/README.md) 是一个专门针对 **XAUUSD (黄金)** 和 **BTCUSD (比特币)** 的端到端智能量化交易系统。它通过多时段 (Multi-Timeframe, MTF) 的自顶向下 (Top-Down) 分析，结合大语言模型 (LLM) 进行市场环境评估，最终在 MetaTrader 5 (MT5) 终端实现自动化执行。

### 核心交易流程：

```
1. 数据获取 ────> 2. SMC 结构检测 ────> 3. AI 市场环境分类 ────> 4. 信号合成 ────> 5. 风险控制 ────> 6. MT5 订单执行
(多时段 Parquet)      (Swing, OB, FVG)       (7-Agent 辩论/ATR)     (Confluence 评分)    (1% 风险/日损熔断)   (BrokerPort 协议)
```

1. **数据准备**：通过 [ForexDataLake](file:///e:/bossquant/MQ5/MQ5/AI-SMC/src/smc/data) 汇聚并重采样 `D1/H4/H1/M15` 级别的 Parquet 历史与实时数据。
2. **结构提取 (SMC Core)**：提取关键的 SMC 指标（前高/前低、流动性扫荡、失衡区 FVG、订单块 OB、结构突破 BOS/CHoCH）。
3. **环境判断 (AI Regime)**：通过 `D1` 和 `H4` 的多因子评估（或调用 7-agent 辩论 pipeline）将市场分类为 5 种状态：`TREND_UP`（趋势上涨）、`TREND_DOWN`（趋势下跌）、`CONSOLIDATION`（区间震荡）、`TRANSITION`（过渡期）、`ATH_BREAKOUT`（历史新高突破）。
4. **信号共振 (Confluence Scoring)**：结合 `H1` 的强区 (Zone) 与 `M15` 级别的入场触发器（如 FVG 填补或流动性扫荡后），综合计算 5 大维度的共振得分。
5. **风险管理 (Risk Guard)**：计算仓位大小（单笔交易不超过账户权益的 1%），并接受每日 3% 亏损熔断和 10% 最大回撤熔断的硬约束。
6. **订单执行**：在 [live_loop.py](file:///e:/bossquant/MQ5/AI-SMC/src/smc/monitor/live_loop.py) 驱动下，通过 [mt5_send.py](file:///e:/bossquant/MQ5/AI-SMC/src/smc/execution/mt5_send.py) 发送至真实的 MT5 账户或 [executor.py](file:///e:/bossquant/MQ5/AI-SMC/src/smc/execution/executor.py) 中的模拟账户。

---

## 二、 项目模块与代码结构 (Modules)

项目采用高度分层的模块化设计，清晰分离了数据、算法、策略、执行和监控。

### 1. 数据源与清洗层 (`src/smc/data/`)

* **核心功能**：屏蔽了底层数据源（如 MT5 实时 API、HistData CSV 等）的差异，实现历史数据的回填（Resampling）和极速 Polars 数据湖读写。
* **文件参考**：[mt5_adapter.py](file:///e:/bossquant/MQ5/AI-SMC/src/smc/data/adapters/mt5_adapter.py)。

### 2. SMC 聪明钱内核 (`src/smc/smc_core/`)

包含 6 大精细化 SMC 模式提取器，是系统技术分析的基石：

* `swing.py`：高低点（Swing High/Low）判定，支持动态周期参数。
* `order_block.py`：订单块生成、强度识别及缓解 (Mitigation) 追踪。
* `fvg.py`：失衡区 FVG 提取、连续 FVG 合并算法。
* `structure.py`：利用高低点突破判定 CHoCH (角色转变) 和 BOS (趋势持续)。
* `liquidity.py`：等高/等低点流动性资金池（EQH/EQL）的扫荡判定。
* `synthetic_zones.py`：合成关键的供需失衡重叠区。

### 3. 多时段策略层 (`src/smc/strategy/`)

* [aggregator.py](file:///e:/bossquant/MQ5/AI-SMC/src/smc/strategy/aggregator.py)：多时段聚合器。`D1→H4` 定趋势大方向，`H1` 扫描可交易 Zone，`M15` 决定 Tick/Bar 级别触发。
* [confluence.py](file:///e:/bossquant/MQ5/AI-SMC/src/smc/strategy/confluence.py)：评分引擎，只有 Confluence 评分超过设定阈值（如 0.6）才会下发 Setup。
* [entry_trigger.py](file:///e:/bossquant/MQ5/AI-SMC/src/smc/strategy/entry_trigger.py)：入场逻辑，支持 OB 测试（OB Retest）、FVG 填补等多种入场触发器。

### 4. LLM 智能风控与状态路由 (`src/smc/ai/`)

* [regime_classifier.py](file:///e:/bossquant/MQ5/AI-SMC/src/smc/ai/regime_classifier.py)：这是系统的精髓。它支持通过宏观 fetcher 获取 DXY（美元指数）、VIX（恐慌指数）、FRED 真实收益率 (TIPS) 等宏观变量。
* **7-Agent 辩论** (`src/smc/ai/debate/`)：当开启 AI 后，系统会启动由不同决策倾向角色（如 Bullish Analyst, Bearish Analyst, Trend Follower 等）组成的智能体辩论，最终由 Judge 裁决。
* [param_router.py](file:///e:/bossquant/MQ5/AI-SMC/src/smc/ai/param_router.py)：根据 AI 划分的 5 种市场 Regime，**动态路由**不同的止损（ATR 乘数）、止盈比例及最大持仓时间。例如：震荡市（Consolidation）下会收窄止损并收紧 TP，突破市（ATH Breakout）下则会放开利润空间。

### 5. 执行与对账层 (`src/smc/execution/`)

* [order_manager.py](file:///e:/bossquant/MQ5/AI-SMC/src/smc/execution/order_manager.py)：订单状态管理与跟踪。
* [reconciler.py](file:///e:/bossquant/MQ5/AI-SMC/src/smc/execution/reconciler.py)：对账引擎。自动处理 Python 端逻辑仓位与 MT5 物理持仓的一致性，防止因网络中断、滑点、手动平仓导致的逻辑死锁。
* [mt5_send.py](file:///e:/bossquant/MQ5/AI-SMC/src/smc/execution/mt5_send.py)：实现自动重试、动态滑点保护和下单“断路器”（Circuit Breaker）。如果 MT5 出现连续 Requote（重新报价）或爆仓风险，自动熔断下单功能。

### 6. 实盘监控环 (`src/smc/monitor/`)

* [live_loop.py](file:///e:/bossquant/MQ5/AI-SMC/src/smc/monitor/live_loop.py)：实盘运行主循环。严格以 `M15` K线收盘为驱动，定期进行对账、风控指标核查和信号生成。
* [mt5_watchdog.py](file:///e:/bossquant/MQ5/AI-SMC/src/smc/monitor/mt5_watchdog.py)：MT5 终端口狗，如果 MT5 连接中断超过阈值，自动启动平仓/断开保护。
* [critical_alerter.py](file:///e:/bossquant/MQ5/AI-SMC/src/smc/monitor/critical_alerter.py)：向 Telegram 推送系统崩溃、熔断触发、实盘成交等关键警报。

---

## 三、 系统的技术优势 (Advantages)

1. **防爆仓多重安全锁 (Surgical Risk Controls)**：
   系统从“单笔风险”、“每日损耗 (3%)”、“历史最大回撤 (10%)”以及“资金利用率 (Margin Cap)”四个维度构建了立体的风控网，甚至在 [state_io.py](file:///e:/bossquant/MQ5/AI-SMC/src/smc/monitor/state_io.py) 中采用了原子写入技术（Atomic Write），即使 VPS 意外断电也能无损恢复状态。
2. **多 Agent 辩论过滤市场环境**：
   相比单模型直接输出，多智能体辩论系统（Claude CLI 7-Agent Debate）能有效剔除 AI 对单一趋势的幻觉，并在大模型调用失败或额度超限时，优雅退化至基于 `ATR/SMA` 的本地数学逻辑分类，确保系统不死机。
3. **“双 Magics” A/B 实盘对照测试**：
   系统设计了极其先进的 [test_strategy_server_dual_magic.py](file:///e:/bossquant/MQ5/AI-SMC/tests/smc/unit/execution/test_strategy_server_dual_magic.py) 架构。在同一个交易账户下，利用不同的 Magic Number 同时跑 `Control Leg` (对照组 - 纯 SMC) 和 `Treatment Leg` (实验组 - AI Regime 增强版 SMC)，支持实盘环境下的秒级科学对照与 Regret Analysis（后悔分析）。
4. **工业级鲁棒性与异常规避**：
   实盘中最头疼的网络延迟和滑点，在 [mt5_send.py](file:///e:/bossquant/MQ5/AI-SMC/src/smc/execution/mt5_send.py) 中利用了基于点数 (Points) 的动态滑点偏离计算（Dynamic Deviation），并实现了断路器和对账器，能从任何突发的终端关闭或手工干预中自动恢复。

---

## 四、 进化路径与性能演进

从 `README.md` 的版本迭代中，我们可以看出整个团队极其严谨的科学回测态度：

* **v1 (Baseline)**: 仅有 9.1% 的胜率。
* **v5 (ATR Adaptive SL)**: 引入 ATR 自适应止损，胜率大幅提高至 38.1%。
* **v6 (Surgical Fixes)**: 禁用 ob_test、收紧过渡区过滤，胜率达到 42.9%，盈亏比提升，PF 达到 1.05。
* **v7 (AI Regime Routing)**: 彻底融入 AI 环境判别路由，参数实现自适应调节，实现了实盘的稳定盈亏比。
* **v9/v10 (Inversion Test)**: 进行了高维反向指标扫描 (`worst_indicator_scan.py`)，证明了团队不仅做策略，还在积极寻找阻碍胜率的“最差因子”，并进行精准规避。

---

### 总结

**AI-SMC** 不是一个简单的“玩具”脚本，而是一个**经历了 10 个 Sprint 迭代、拥有完整 walk-forward out-of-sample (前向外推) 验证、内嵌 AI Regime 路由、并且具备 A/B 双魔术编码实盘对账能力的工业级量化交易系统**。其代码结构极度严谨，测试覆盖率（Unit/Integration/Property-based）覆盖了几乎所有极端边界情况。

如果您有任何想要深入修改、优化或调试的具体模块（例如 Martingale 增强、AI 路由条件调整等），我们可以直接开始！
